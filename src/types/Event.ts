type Event = {
  dogId: string;
  grade: number;
  height: string;
  userId: string;
  eventName: string;
  eventType: string;
  dateOfEvent: string;
  league: string;
  points: number;
};

export default Event;

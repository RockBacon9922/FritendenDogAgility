name: TypeCheck
on:
  push:
  pull_request:

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.3.0
      - uses: actions/setup-node@3.6.0
        with:
          node-version: 12
      - run: npm install
      - run: npx tsc --noEmit
